export GITHUB_ACCOUNT=pcpetty

echo ${GITHUB_ACCOUNT}


git clone https://github.com/pcpetty/ci-cd-final-project.git

cd ci-cd-final-project

bash ./bin/setup.sh

exit


-------

git config --global user.email colepetty57@gmail.com

git config --global user.name pcpetty

git add -A
git commit -m "COMMIT MESSAGE"
git push